{{- if .Values.preInstall.enabled }}
---
# Note: No custom RBAC needed for Kubernetes auth
# Service account JWT token is automatically mounted and used for Vault authentication
# Vault handles authorization through the secret-generator role and policy
{{- end }}