# Wrapper chart for alloy
# Defaults here, environment-specific overrides in values/homelab.yaml

alloy:
  controller:
    type: daemonset

  alloy:
    # Mount /var/log for pod logs
    mounts:
      varlog: true
