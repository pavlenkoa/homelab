# Loki configuration for homelab cluster
# Environment-specific overrides only

httproute:
  routes:
    - name: loki
      hostname: loki.pavlenko.io
      gateway:
        name: homelab
        namespace: kgateway-system
      service:
        name: loki
        port: 3100

loki: {}
