apiVersion: v1
kind: Service
metadata:
  name: mktxp
  namespace: monitoring
  labels:
    app: mktxp
spec:
  selector:
    app: mktxp
  ports:
    - port: 49090
      targetPort: metrics
      name: metrics
