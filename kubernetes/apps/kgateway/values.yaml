# kgateway defaults
kgateway:
  controller:
    replicaCount: 1

# Self-managed Envoy proxy configuration
envoyProxy:
  imageRepository: cr.kgateway.dev/kgateway-dev/envoy-wrapper
  nodeSelector:
    kubernetes.io/hostname: macmini

# Gateway configuration
gateway:
  name: homelab
  listeners:
    http:
      port: 80
    https:
      port: 443
