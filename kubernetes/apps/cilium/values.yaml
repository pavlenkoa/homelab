# Wrapper chart for cilium
# Defaults here, environment-specific overrides in values/homelab.yaml

cilium:
  kubeProxyReplacement: true

  # Disable envoy - RPi4 doesn't support 48-bit VA that tcmalloc requires
  envoy:
    enabled: false

  prometheus:
    enabled: true

  operator:
    replicas: 1
    prometheus:
      enabled: true

  hubble:
    relay:
      enabled: true
    ui:
      enabled: true
